# 开发日志 - 2026-08-08

## 今日完成事项

### 编译验证与修复
- [x] 修正 `gradle.properties` 中 Java 路径: Eclipse Adoptium → `C:/Program Files/Zulu/zulu-21`
  - 原因: 原路径在本机不存在，Zulu JDK 21 为实际安装路径
- [x] 修正 `gradle/wrapper/gradle-wrapper.properties`: Gradle 9.5 → 9.6.1
  - 原因: 9.5 发行版下载 404 且本地缓存不完整；9.6.1 本地缓存完整，与开发日志 2026-08-07 记录一致
- [x] 移除 `build.gradle` 中 `loom { splitEnvironmentSourceSets() }`
  - 原因: 项目源码全部位于 `src/main/java` 且均为客户端代码，splitEnvironmentSourceSets 模式下 `main` source set 不含 Minecraft 客户端类，导致 `net.minecraft.client.*` 与 Fabric client API 无法解析
- [x] 修复 `ClientPlayerEntityMixin.java` 中 `new Identifier("minecraft", "the_end")` → `Identifier.of("minecraft", "the_end")`
  - 原因: 1.21.11 中 `Identifier(String, String)` 构造函数已变为 private，须使用静态工厂方法
- [x] 修复 `ClientPlayerEntityMixin.java` 中 2 处 `player.getWorld()` → `player.getEntityWorld()`
  - 原因: Yarn 1.21.2+ 将 `Entity.getWorld()` 重命名为 `getEntityWorld()`（通过 javap 分析 named jar 确认）
- [x] 执行完整构建 — **BUILD SUCCESSFUL** (37s)
- [x] 验证 JAR 生成:
  - `mbtfly-1.0.0.jar` (14,713 bytes)
  - `mbtfly-1.0.0-sources.jar` (8,843 bytes)
  - `mbtfly-1.0.0-javadoc.jar` (104,464 bytes)

## 遇到的问题

1. **Gradle Wrapper 下载失败**: wrapper 配置的 Gradle 9.5 发行版下载 404，且本地缓存仅剩 `.part` 残留。改用本地缓存完整的 Gradle 9.6.1 直接构建。
2. **编译 60 个错误（首次）**: `net.minecraft.client`、Fabric client API 包不存在。根因为 `splitEnvironmentSourceSets()` 使 `main` source set 不含客户端类。
3. **`Identifier` 构造函数 private**: 1.21.11 API 变更，改用 `Identifier.of()`。
4. **`getWorld()` 重命名**: 与 `getPos()` → `getEntityPos()` 同批次变更，1.21.11 中为 `getEntityWorld()`。

## 变更记录摘要

| 文件 | 变更 | 原因 |
|------|------|------|
| `gradle.properties` | Java 路径 → Zulu zulu-21 | 原 Eclipse Adoptium 路径不存在 |
| `gradle-wrapper.properties` | Gradle 9.5 → 9.6.1 | 9.5 下载 404，9.6.1 本地缓存完整 |
| `build.gradle` | 移除 splitEnvironmentSourceSets() | 纯客户端项目，main source set 需含客户端类 |
| `ClientPlayerEntityMixin.java` | Identifier 构造 → Identifier.of() | 1.21.11 构造函数 private |
| `ClientPlayerEntityMixin.java` | getWorld() → getEntityWorld() (2处) | Yarn 1.21.2+ 方法重命名 |

## 下一步计划

- [ ] 将模组 JAR 部署至游戏 mods 目录进行实机测试
- [ ] 验证飞行、暂停、自动退出等核心功能
